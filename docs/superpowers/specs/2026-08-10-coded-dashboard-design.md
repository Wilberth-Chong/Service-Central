# Coded Services Central dashboard

## Scope

Replace the `dashboard.png` implementation with a native, pixel-matched 1440px Services Central dashboard. Other prototype routes remain screenshot-backed.

## Layout

Build the top chrome, sidebar, dashboard heading, access-request notice, search, four shortcut cards, promotion banner, support-ticket section, and favorite-instrument cards with native semantic HTML and CSS. Use real icons and the repository's existing instrument imagery; do not render `dashboard.png`.

## Interaction

Keep the existing navigation destinations. Make dashboard search, ticket tabs, status filter, pagination, promotional carousel controls, and instrument-card request actions operate on local dashboard data.

## Verification

At a 1440px desktop viewport, compare the native dashboard with the existing dashboard reference. Confirm route controls, local dashboard controls, keyboard focus, and responsive scaling work.
